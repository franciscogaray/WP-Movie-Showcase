# WP-Movie-Showcase
WP Movie Showcase lets editors embed rich movie information into any post or page through a dedicated Gutenberg block. Movie data is fetched from the OMDb API (https://www.omdbapi.com/) and stored alongside the block, so the frontend renders instantly without runtime API calls.
